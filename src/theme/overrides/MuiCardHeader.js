import palette from '../palette';
import typography from '../typography';

export default {
  root: {
    padding: '12px',
    backgroundColor: palette.background.widgetHeader,
  },
  title: {
    ...typography.h1_title,
  },
};
